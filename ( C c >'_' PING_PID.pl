#!/usr/local/bin/perl
use strict; use warnings;
##############################
# PING_PID - test minion state
#  ( C c >'_'  ---skrp of MKRX
# default list minnion | test
# if argv test those only

sub ping {
  my ($proc) = @_;
  
}
